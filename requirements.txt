numpy==1.26.4
scipy==1.13.0
# pymc constraint relaxed: bambi 0.18 requires pymc>=6, which conflicts with the original 5.13.1 pin.
# Allow pymc 6.x; bambi resolves the exact compatible build at install time.
pymc>=6.0,<7
matplotlib>=3.9.0
bambi==0.18.0
nutpie==0.16.10
scikit-learn==1.4.2
